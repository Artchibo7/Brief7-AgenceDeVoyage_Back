{% extends 'base.html.twig' %}

{% block title %}Art & Voyage{% endblock %}

{% block body %}
    <h1 class="text-center" style="color: green">Catégorie</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr> #}
            <tr>
                <th>Description</th>
                <td>{{ categorie.Description }}</td>
            </tr>
            <tr>
                <th>Nom catégorie</th>
                <td>{{ categorie.NomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_categorie_index') }}">retour à la liste</a>

    <a class="btn btn-success" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">éditer</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
