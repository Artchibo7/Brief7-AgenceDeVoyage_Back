{% extends 'base.html.twig' %}

{% block title %}Art & Voyage{% endblock %}

{% block body %}
    <h1 class="text-center" style="color: green">Les destinations</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr> #}
            <tr>
                <th>NomPays</th>
                <td>{{ destination.NomPays }}</td>
            </tr>
            <tr>
                <th>NomVille</th>
                <td>{{ destination.NomVille }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ destination.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_destination_index') }}">retour à la liste</a>

    <a class="btn btn-success" href="{{ path('app_destination_edit', {'id': destination.id}) }}">éditer</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
