{% extends 'base.html.twig' %}

{% block title %}Art & Voyage{% endblock %}

{% block body %}
    <h1 class="text-center" style="color: green">Voyage</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ voyage.id }}</td>
            </tr> #}
            <tr>
                <th>NomVoyage</th>
                <td>{{ voyage.NomVoyage }}</td>
            </tr>
            <tr>
                <th>DateDepart</th>
                <td>{{ voyage.DateDepart ? voyage.DateDepart|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateRetour</th>
                <td>{{ voyage.DateRetour ? voyage.DateRetour|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voyage.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_voyage_index') }}">retour à la liste</a>


    {{ include('voyage/_delete_form.html.twig') }}
{% endblock %}
