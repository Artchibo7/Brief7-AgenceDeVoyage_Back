{% extends 'base.html.twig' %}

{% block title %}Réservations{% endblock %}

{% block body %}
    <h1 class="text-center" style="color: green">Les réservations</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr> #}
            <tr>
                <th>Nom</th>
                <td>{{ reservation.Nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ reservation.Prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reservation.Email }}</td>
            </tr>
            <tr>
                <th>Téléphone</th>
                <td>{{ reservation.Telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info my-3" href="{{ path('app_reservation_index') }}">retour à la liste</a>

    <a class="btn btn-success my-3" href="{{ path('app_reservation_edit', {'id': reservation.id}) }}">éditer</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
